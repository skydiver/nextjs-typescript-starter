const Home = () => (
  <div className="w-screen h-screen px-8 flex items-center justify-center font-semibold text-3xl text-center">
    Next.js starter template with TypeScript + Tailwind CSS
  </div>
);

export default Home;
